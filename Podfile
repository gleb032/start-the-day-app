# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Start the day app' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Start the day app
  pod 'SwiftLint'

  target 'Start the day appTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Start the day appUITests' do
    # Pods for testing
  end

end
